<h1>New army</h1>

<% form_for([:admin,@army],:method=>:post) do |f| %>
  <%= f.error_messages %>
	<%= f.label :name %>
	<%= f.text_field :name %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_path %>
