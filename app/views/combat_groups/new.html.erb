<h1>New combat_group</h1>

<% form_for(@combat_group) do |f| %>
  <%= f.error_messages %>
	<%=f.hidden_field :army_list_id,:value=>params[:army_list_id]%>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', combat_groups_path %>
