<h1>Listing unit_options</h1>

<table>
  <tr>
  </tr>

<% for unit_options in @unit_options %>
  <tr>
    <td><%= link_to 'Show', unit_options %></td>
    <td><%= link_to 'Edit', edit_unit_options_path(unit_options) %></td>
    <td><%= link_to 'Destroy', unit_options, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New unit_options', new_unit_options_path %>
